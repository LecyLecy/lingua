# Experiments

Keep executable experiment definitions, commands, and curated provenance here. Generated runs, datasets, checkpoints, and model weights are ignored.

Every recorded result must identify data split, model/runtime, decoding settings, hardware, command, metric definition, output path, and limitations.
